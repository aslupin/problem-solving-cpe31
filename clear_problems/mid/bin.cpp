#include <iostream>
#include <stdio.h>
using namespace std;
int main()
{
    
    int n;
    int operand;
    char command;
    
    // cin >> n;
    scanf("%d", &n);
    for (int i = 0; i < n; i++)
    {
        scanf(" %c %d", &command, &operand);
       
    }
    cout << 0;
}