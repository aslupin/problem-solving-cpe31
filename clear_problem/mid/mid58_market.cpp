#include <iostream>

#include <vector>
using namespace std;
int main()
{
    char typePerson;
    int n, , mami, g;
    scanf("%d", &n);
    vector<int> seller, buyer;
    for (int m = 0; m < n; m++)
    {
        scanf(" %c %d %d", &typePerson, &mami, &g);
    }
}
