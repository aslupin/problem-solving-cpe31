#include <iostream>
#include <stdio.h>
#include <vector>
using namespace std;
int main()
{
    vector<int> mybst;
    int n, operand;
    char command;
    // cin >> n;
    scanf("%d", &n);
    for (unsigned i = 0; i < n; i++)
    {
        scanf(" %c %d", &command, &operand);
        if (command == '1')
        {
        }
    }
}